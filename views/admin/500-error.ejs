<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>CRUX |500 - Internal Server Error</title>
    <style>
      :root {
        --primary-color: #e53e3e;
        --secondary-color: #3182ce;
        --background-color: #f7f7f7;
        --card-background: #ffffff;
        --text-color: #333333;
        --text-muted: #666666;
        --border-radius: 12px;
        --shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1),
          0 4px 6px -2px rgba(0, 0, 0, 0.05);
      }

      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
      }

      body {
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
          Oxygen, Ubuntu, Cantarell, "Open Sans", "Helvetica Neue", sans-serif;
        background-color: var(--background-color);
        color: var(--text-color);
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        padding: 20px;
        line-height: 1.6;
      }

      .alert {
        width: 100%;
        max-width: 600px;
        margin-bottom: 20px;
        padding: 12px 16px;
        border-radius: var(--border-radius);
        position: relative;
      }

      .alert-success {
        background-color: #d4edda;
        color: #155724;
        border: 1px solid #c3e6cb;
      }

      .alert-danger {
        background-color: #f8d7da;
        color: #721c24;
        border: 1px solid #f5c6cb;
      }

      .alert-dismissible {
        padding-right: 40px;
      }

      .btn-close {
        position: absolute;
        top: 12px;
        right: 16px;
        background: transparent;
        border: none;
        font-size: 20px;
        cursor: pointer;
        color: inherit;
        opacity: 0.5;
      }

      .btn-close:hover {
        opacity: 1;
      }

      .error-container {
        width: 100%;
        max-width: 600px;
        padding: 40px;
        background-color: var(--card-background);
        border-radius: var(--border-radius);
        box-shadow: var(--shadow);
        text-align: center;
      }

      .error-icon {
        width: 120px;
        height: 120px;
        margin: 0 auto 24px;
      }

      .error-icon svg {
        width: 100%;
        height: 100%;
        fill: var(--primary-color);
      }

      h1 {
        font-size: 32px;
        margin-bottom: 16px;
        color: var(--primary-color);
      }

      .error-code {
        display: inline-block;
        background-color: #f5f5f5;
        border-radius: 6px;
        padding: 6px 12px;
        font-family: monospace;
        font-size: 18px;
        margin-bottom: 24px;
        border: 1px solid #e0e0e0;
      }

      p {
        font-size: 18px;
        margin-bottom: 16px;
      }

      .actions {
        margin-top: 32px;
        display: flex;
        flex-direction: column;
        gap: 16px;
      }

      .home-button {
        display: inline-block;
        background-color: var(--secondary-color);
        color: white;
        padding: 12px 24px;
        border-radius: 6px;
        text-decoration: none;
        font-weight: 500;
        transition: background-color 0.2s, transform 0.1s;
      }

      .home-button:hover {
        background-color: #2c5282;
        transform: translateY(-2px);
      }

      .retry-button {
        display: inline-block;
        background-color: transparent;
        color: var(--secondary-color);
        padding: 12px 24px;
        border-radius: 6px;
        text-decoration: none;
        font-weight: 500;
        border: 1px solid var(--secondary-color);
        transition: background-color 0.2s, transform 0.1s;
      }

      .retry-button:hover {
        background-color: rgba(49, 130, 206, 0.1);
        transform: translateY(-2px);
      }

      .server-info {
        margin-top: 40px;
        font-size: 14px;
        color: var(--text-muted);
      }

      .support-info {
        margin-top: 16px;
        font-size: 14px;
        color: var(--text-muted);
      }

      @media (max-width: 640px) {
        .error-container {
          padding: 30px 20px;
        }

        .error-icon {
          width: 80px;
          height: 80px;
        }

        h1 {
          font-size: 28px;
        }

        p {
          font-size: 16px;
        }
      }
    </style>
  </head>
  <body>
    <% if (messages && messages.success) { %>
    <div class="alert alert-success alert-dismissible fade show" role="alert">
      <%= messages.success %>
      <button
        type="button"
        class="btn-close"
        data-bs-dismiss="alert"
        aria-label="Close"
      >
        ×
      </button>
    </div>
    <% } %> <% if (messages && messages.error) { %>
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
      <%= messages.error %>
      <button
        type="button"
        class="btn-close"
        data-bs-dismiss="alert"
        aria-label="Close"
      >
        ×
      </button>
    </div>
    <% } %>

    <div class="error-container">
      <div class="error-icon">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
          <path
            d="M12 4c4.411 0 8 3.589 8 8s-3.589 8-8 8-8-3.589-8-8 3.589-8 8-8zm0-2c-5.523 0-10 4.477-10 10s4.477 10 10 10 10-4.477 10-10-4.477-10-10-10zm0 14.5c.828 0 1.5-.672 1.5-1.5s-.672-1.5-1.5-1.5-1.5.672-1.5 1.5.672 1.5 1.5 1.5zm0-9c.552 0 1 .448 1 1v4c0 .552-.448 1-1 1s-1-.448-1-1v-4c0-.552.448-1 1-1z"
          />
        </svg>
      </div>

      <h1>Internal Server Error</h1>
      <div class="error-code">500</div>

      <p>We're sorry, but something went wrong on our server.</p>
      <p>Our team has been notified and we're working to fix the issue.</p>

      <div class="actions">
        <a href="/" class="home-button">Return to Homepage</a>
        <a href="javascript:location.reload()" class="retry-button"
          >Try Again</a
        >
      </div>

      <div class="server-info"></div>

      <div class="support-info">
        If this problem persists, please contact
        <a href="mailto:support@example.com">support@crux.com</a>
      </div>
    </div>

    <script>
      // Close alert when close button is clicked
      document.addEventListener("DOMContentLoaded", function () {
        const closeButtons = document.querySelectorAll(".btn-close");
        closeButtons.forEach((button) => {
          button.addEventListener("click", function () {
            const alert = this.closest(".alert");
            alert.style.display = "none";
          });
        });
      });
    </script>
  </body>
</html>
